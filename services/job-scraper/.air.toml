root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/server"
  cmd = "go build -o ./tmp/server ./cmd/server/main.go"
  include_ext = ["go"]
  exclude_dir = ["tmp"]
  delay = 500

[log]
  time = false

[color]
  build = "yellow"
  runner = "green"
  watcher = "cyan"
